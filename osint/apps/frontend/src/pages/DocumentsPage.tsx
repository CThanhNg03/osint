/** Page for document management feature. */
import React from 'react';
import DocumentsPanel from '../features/documents/DocumentsPanel';

const DocumentsPage: React.FC = () => (
  <section>
    <DocumentsPanel />
  </section>
);

export default DocumentsPage;
