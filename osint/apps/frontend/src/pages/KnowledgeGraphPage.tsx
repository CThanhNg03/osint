/** Page for knowledge graph exploration. */
import React from 'react';
import GraphSearch from '../features/kg/GraphSearch';

const KnowledgeGraphPage: React.FC = () => (
  <section>
    <GraphSearch />
  </section>
);

export default KnowledgeGraphPage;
