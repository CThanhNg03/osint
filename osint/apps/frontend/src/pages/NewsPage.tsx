/** Page that surfaces the news feature. */
import React from 'react';
import NewsPanel from '../features/news/NewsPanel';

const NewsPage: React.FC = () => (
  <section>
    <NewsPanel />
  </section>
);

export default NewsPage;
