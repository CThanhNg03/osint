/** Page for the live monitoring feature. */
import React from 'react';
import LivePanel from '../features/live/LivePanel';

const LiveMonitorPage: React.FC = () => (
  <section>
    <LivePanel />
  </section>
);

export default LiveMonitorPage;
