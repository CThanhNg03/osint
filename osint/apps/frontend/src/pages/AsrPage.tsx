/** Page for ASR uploads. */
import React from 'react';
import AsrPanel from '../features/asr/AsrPanel';

const AsrPage: React.FC = () => (
  <section>
    <AsrPanel />
  </section>
);

export default AsrPage;
